package xyz.germanfica._2019._1st.tpfinal;

public class Aleatorio {
	/**
	 * Crear un TDA “Aleatorio” que guarde un número con la siguiente información:
	 * a. Número aleatorio entero. b. Atributo que diga si tiene dígitos gemelos
	 * (resolverlo por recursión). c. Atributo que diga si pertenece a la serie de
	 * Fibonacci (resolverlo por recursión). d. Los metodos observadores deben
	 * incluir a equals(), toString() y compareTo(). e. Implemente el método
	 * clonar(), que retorna un nuevo objeto Aleatorio que es copia del objeto
	 * Aleatorio al que le envió el mensaje.
	 */
}
